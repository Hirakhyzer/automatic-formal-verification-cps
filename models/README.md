# Model inputs

The executable v0.1 models live in `src/cpsverify/domains/` so their assumptions are versioned with the verification code. This directory is reserved for future declarative JSON/YAML model descriptions and converter experiments.
