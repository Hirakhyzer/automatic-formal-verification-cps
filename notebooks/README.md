# Notebooks

Notebooks are intentionally not required for the core verification workflow. Reproducible command-line scripts are preferred for benchmark claims. Future notebooks should consume machine-readable results rather than contain hidden experimental logic.
