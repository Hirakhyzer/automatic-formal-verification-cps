# Benchmarks

`V0_1_BASELINE.md` records the locally reproduced baseline for the repository. Generated benchmark JSON is written to `results/` and is intentionally ignored by Git except for documentation.
